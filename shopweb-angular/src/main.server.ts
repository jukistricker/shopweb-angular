import { bootstrapApplication } from '@angular/platform-browser';
import { OrderComponent } from './app/order/order.component'

const bootstrap = () => bootstrapApplication(OrderComponent);

export default bootstrap;
